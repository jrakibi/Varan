{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'Social information creation'|trans }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('socialinformation_index') }}">{{ 'Back to the list'|trans }}</a>
        </li>
    </ul>
{% endblock %}
