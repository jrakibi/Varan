{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'Reason'|trans }}</h1>

    <table>
        <tbody>
            <tr>
                <th>{{ 'Id'|trans }}</th>
                <td>{{ reason.id }}</td>
            </tr>
            <tr>
                <th>{{ 'Reason'|trans }}</th>
                <td>{{ reason.reason }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('reasons_index') }}">{{ 'Back to the list'|trans }}</a>
        </li>
        <li>
            <a href="{{ path('reasons_edit', { 'id': reason.id }) }}">{{ 'Edit'|trans }}</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
